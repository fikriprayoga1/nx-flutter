import 'package:flutter/material.dart';

class WelcomeWidgetCard extends StatelessWidget {
  const WelcomeWidgetCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Welcome Widget Card');
  }
}
